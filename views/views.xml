<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="jwt_provider.list">
      <field name="name">jwt_provider list</field>
      <field name="model">jwt_provider.jwt_provider</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="jwt_provider.action_window">
      <field name="name">jwt_provider window</field>
      <field name="res_model">jwt_provider.jwt_provider</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="jwt_provider.action_server">
      <field name="name">jwt_provider server</field>
      <field name="model_id" ref="model_jwt_provider_jwt_provider"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="jwt_provider" id="jwt_provider.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="jwt_provider.menu_1" parent="jwt_provider.menu_root"/>
    <menuitem name="Menu 2" id="jwt_provider.menu_2" parent="jwt_provider.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="jwt_provider.menu_1_list" parent="jwt_provider.menu_1"
              action="jwt_provider.action_window"/>
    <menuitem name="Server to list" id="jwt_provider" parent="jwt_provider.menu_2"
              action="jwt_provider.action_server"/>
    -->
  </data>
</odoo>